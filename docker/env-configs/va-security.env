# dedicated app role flyway provisions (V1601908413__setup_users.sql), not the
# postgres superuser in postgres.env - that role's search_path excludes "acuity"
POSTGRES_USER=acuity
POSTGRES_PASSWORD=test
